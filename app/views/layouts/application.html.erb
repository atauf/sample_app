<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
  	<%= render 'layouts/header' %>
  	<div class="container">
  		<%= yield %>  <!-- this is the code that is responsible for inserting the contents of each page into it.  It takes from the VIEWS with the same name -->
  		<%= render 'layouts/footer' %>
  	</div>
  </body>
</html>

